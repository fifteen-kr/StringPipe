import type { PipeCategory } from "../type";

export const PIPES_HASH: PipeCategory = {
    id: 'hash',
    name: "Hash & Checksum",
    description: "Hash and checksum functions.",

    pipes: [],
};